\ProvidesPackage{usn_python}

\ExecuteOptions{norsk}
\ExecuteOptions{singlespacing}
\ExecuteOptions{parskip}

\RequirePackage{hhline}
\RequirePackage[subpreambles=true]{standalone}
\RequirePackage[T1]{fontenc}
\RequirePackage[norsk]{babel}
\RequirePackage[parfill]{parskip}
\RequirePackage[table, dvipsnames]{xcolor}
\RequirePackage{graphicx}
\RequirePackage{wrapfig}
\RequirePackage{fancyhdr} %for header og footer
\RequirePackage{import}
\RequirePackage{geometry}
\RequirePackage[backend=biber, style=numeric, sorting=none]{biblatex}
\RequirePackage[autostyle=true]{csquotes}
\RequirePackage{setspace}
\RequirePackage{hyperref}
\RequirePackage{amsmath}
\RequirePackage{latexsym}
\RequirePackage{color}
\RequirePackage{todonotes}
\RequirePackage{fontspec}
\RequirePackage{array, multirow}
\RequirePackage{hhline}
\RequirePackage{listings}
\RequirePackage{color}

\ProcessOptions\relax

\setmainfont[Path=C:/Windows/Fonts/,
    BoldItalicFont=calibriz.ttf,
    BoldFont      =calibrib.ttf,
    ItalicFont    =calibrii.ttf]{calibri.ttf}
\geometry{
	paper=a4paper, 
	inner=4cm, 
	outer=2cm, 
	top=2cm, 
	bottom=2.54cm,
}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[LE, RO]{\rightmark}
\fancyhead[LO, RE]{\leftmark}
\cfoot{Page \thepage}
\addbibresource{bibliotek.bib} % Legg til filnavn på bibliotek
\title{Python-kurs\\
Førsteårs ingeniørstudenter USN Kongsberg}
\author{Joakim Bjørk \& Børge Kile Gjelsten}
\setlength{\marginparwidth}{3cm}
\reversemarginpar
\definecolor{dkgreen}{rgb}{0,0.6,0}
\definecolor{gray}{rgb}{0.5,0.5,0.5}
\definecolor{mauve}{rgb}{0.58,0,0.82}

\lstset{frame=tb,
  language=Python,
  aboveskip=3mm,
  belowskip=3mm,
  showstringspaces=false,
  columns=flexible,
  basicstyle={\small\ttfamily},
  numbers=none,
  numberstyle=\tiny\color{gray},
  keywordstyle=\color{blue},
  commentstyle=\color{dkgreen},
  stringstyle=\color{mauve},
  breaklines=true,
  breakatwhitespace=true,
  tabsize=3
}

\endinput